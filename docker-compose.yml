services:
  bot:
    build: .
    image: ghcr.io/bravexistence/drinks_to_ass_bot:latest
    container_name: drinks-to-ass-bot
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
